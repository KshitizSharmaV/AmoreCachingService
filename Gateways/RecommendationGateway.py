# def function(userIdCalculateRecommendedPrfoilesFor=profileId):
    if firebase is :
        R1 = GET ALL TOP SCORED PROFILES
        a. R1 = Profiles Not already seen by user
        a. R1 = Radius=50 && Filter= 
        b. R2 = R1 is not Blocked
        c. R3 = Arrange the deck according to people score(Grade: Completion Score(20), LikesDislikes=)
        d. R4 = We arrange the deck order
        e. R5 = send profile back
        storedInCache()
        firebaseStore()
        return RecommenDatedProfiles
    return 
